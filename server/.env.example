# Backend Environment Variables

# Server Configuration
PORT=3000

# Database Configuration (PostgreSQL for both dev and prod)
# For local development, you can use a local PostgreSQL instance or a free cloud database
DATABASE_URL=postgresql://username:password@localhost:5432/friendship_quiz

# For production
# DATABASE_URL=postgresql://username:password@host:port/database
